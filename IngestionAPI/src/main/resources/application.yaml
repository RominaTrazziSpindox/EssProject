spring:
  application:
    name: ingestion-api
  RabbitMQ:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  rabbit:
    exchange: crm.exchange
    routing-key: crm.campaign.created

logging:
  level:
    org.springframework.amqp: DEBUG

security:
  api-key: secret-key

server:
  port: 8080